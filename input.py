#! /usr/bin/python3

name = input("What is your name ? ")
age = input("How old are you ? ")

print ("Hello " + name + " are you " + age + " years old ? ")

response = input("So " + name + " once again, are you really " + age + " years old ?? ")

print("Mmm really, I don't trust that answer '" + response + "'")

nothing = input()
print ("Printing what was asking via input nothing " + nothing)
